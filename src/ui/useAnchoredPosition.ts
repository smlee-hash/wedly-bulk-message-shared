/**
 * 껍데기 — 실물은 공용 꾸러미 `@wedly/ui-shared` 의 `src/ui/useAnchoredPosition.ts` 에 있다.
 * 부품 묶음(`/ui`)이 아니라 전용 통로로 부른다 — 묶음으로 부르면 부품 40개가 딸려 들어온다.
 */
export { useAnchoredPosition } from "@wedly/ui-shared/ui/useAnchoredPosition";
